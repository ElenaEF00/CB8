import styles from "./index.module.scss";

const Hero = () => {
  return (
    <div className={styles.Hero}>
      <h1 className={styles.title}>Scopri un nuovo modo di vedere il mondo</h1>
    </div>
  );
};

export default Hero;
